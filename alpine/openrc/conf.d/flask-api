# /etc/conf.d/flask-api - defaults for Flask API service
# Copy this file to /etc/conf.d/flask-api and adjust as needed

# Where the Flask app (app.py) resides (contains app:app)
APP_DIR="/opt/grow/api"

# Bind address for gunicorn (stay on loopback, exposed via Caddy)
BIND="127.0.0.1:5000"

# Gunicorn workers
WORKERS="2"

# User/group running the service
USER="grow"
GROUP="grow"

# Logs and pid
LOG_DIR="/var/log/grow"
PIDFILE="/run/flask-api.pid"
